---
# Optional underconstrained attack module
# NOTE: uses a stub campaign to satisfy v2 include parsing.
campaign:
  name: ""
  version: "2.0"
  target:
    framework: "mock"
    circuit_path: ""
    main_component: ""

attacks:
  - type: "underconstrained"
    description: "Detect multiple valid witnesses for same public output"
    config:
      witness_pairs: 50
      # Optional public input scoping (choose one)
      # public_input_names: ["root", "nullifier"]
      # public_input_positions: [0, 1]
      # public_input_count: 2
      # Optional fixed public input values (must match public input list)
      # fixed_public_inputs: ["0x0", "0x0"]
