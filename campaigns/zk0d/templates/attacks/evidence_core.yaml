---
# Shared evidence-mode core attacks
# NOTE: uses a stub campaign to satisfy v2 include parsing.
campaign:
  name: ""
  version: "2.0"
  target:
    framework: "circom"
    circuit_path: ""
    main_component: ""

attacks:
  - type: "constraint_inference"
    description: "Detect missing constraints with confirmation"
    config:
      confidence_threshold: 0.7
      confirm_violations: true

  - type: "boundary"
    description: "Boundary value testing"
    config:
      test_values:
        - "0"
        - "1"
        - "p-1"
