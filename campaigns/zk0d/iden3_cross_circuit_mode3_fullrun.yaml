---
includes:
  - "iden3_cross_circuit_mode3.yaml"

campaign:
  name: ""
  version: "2.0"
  target:
    framework: "circom"
    circuit_path: ""
    main_component: ""

attacks:
  - type: "constraint_inference"
    description: "Detect missing constraints with confirmation"
    config:
      confidence_threshold: 0.7
      confirm_violations: true

  - type: "boundary"
    description: "Boundary value testing"
    config:
      test_values:
        - "0"
        - "1"
        - "p-1"

  - type: "soundness"
    description: "Strict readiness soundness attack"
    config:
      forge_attempts: 1000

  - type: "underconstrained"
    description: "Strict readiness underconstrained attack"
    config:
      public_input_count: 1

  - type: "metamorphic"
    description: "Strict readiness metamorphic oracle"
    config: {}

  - type: "constraint_slice"
    description: "Strict readiness constraint slice"
    config: {}

  - type: "spec_inference"
    description: "Strict readiness spec inference"
    config: {}

  - type: "witness_collision"
    description: "Strict readiness witness collision"
    config: {}
