version: "1.0"

registries:
  local_benchmark_patterns:
    path: "campaigns/benchmark/patterns"
    description: "Strict generic benchmark patterns (no selector gating)"
    maintainer: "ZkPatternFuzz"

collections:
  benchmark_generic:
    registry: local_benchmark_patterns
    description: "Generic strict benchmark template set"
    templates:
      - underconstrained_strict_probe.yaml

  benchmark_merkle_specific:
    registry: local_benchmark_patterns
    description: "Merkle-specific benchmark template set"
    templates:
      - merkle_path_binarity_probe.yaml

aliases:
  benchmark: ["benchmark_generic"]
  benchmark_generic: ["benchmark_generic"]
